{
  "name": "zero-app-playwright",
  "version": "1.0.0",
  "description": "Playwright + TS smoke tests",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.1",
    "@types/node": "^25.2.1",
    "typescript": "^5.6.0"
  }
}
